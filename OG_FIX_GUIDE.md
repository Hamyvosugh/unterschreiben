# ✅ مشکل تصویر واتساپ برطرف شد!

## تغییرات اعمال شده:

### 1. ✅ اضافه شدن `metadataBase`

```typescript
metadataBase: new URL("https://iranwing.com");
```

این خط به Next.js میگه که base URL سایت چیه و تمام URLهای نسبی رو به absolute تبدیل کنه.

### 2. ✅ تصحیح URL تصویر Open Graph

**قبل:**

```typescript
images: ["https://iranwing.com/images/og-image.jpg"];
```

**بعد:**

```typescript
images: ["/opengraph-image"];
```

حالا به تصویر dynamic که خود Next.js میسازه اشاره میکنه.

### 3. ✅ بهبود تصویر OG

- افزودن emoji و icon
- gradient زیباتر
- shadow و styling بهتر
- نمایش دامنه سایت

## 📱 تست در واتساپ:

### مرحله 1: Deploy کنید

```bash
git add .
git commit -m "Fix Open Graph preview for WhatsApp"
git push
```

### مرحله 2: پاک کردن Cache

واتساپ تصاویر را cache میکنه. برای تست:

1. **روش 1 - استفاده از Facebook Debugger:**
   - برو به: https://developers.facebook.com/tools/debug/
   - URL سایتت رو وارد کن
   - روی "Scrape Again" کلیک کن
   - این کار cache رو پاک میکنه

2. **روش 2 - اضافه کردن Query String:**

   ```
   https://iranwing.com?v=1
   https://iranwing.com?test=1
   ```

   این باعث میشه واتساپ فکر کنه URL جدیده و دوباره fetch کنه.

3. **روش 3 - صبر کنید:**
   - Cache واتساپ معمولاً بعد از 24-48 ساعت منقضی میشه
   - یا چند دقیقه صبر کنید و دوباره امتحان کنید

### مرحله 3: تست در پلتفرم‌های مختلف

- ✅ واتساپ
- ✅ تلگرام
- ✅ توییتر
- ✅ فیسبوک
- ✅ لینکدین

## 🔍 ابزارهای تست آنلاین:

1. **Facebook Sharing Debugger** (بهترین!)
   https://developers.facebook.com/tools/debug/

2. **OpenGraph.xyz**
   https://www.opengraph.xyz/

3. **Twitter Card Validator**
   https://cards-dev.twitter.com/validator

4. **LinkedIn Post Inspector**
   https://www.linkedin.com/post-inspector/

## ✅ چک‌لیست:

- [x] metadataBase تنظیم شده
- [x] opengraph-image.tsx وجود دارد
- [x] URL تصویر به /opengraph-image اشاره میکنه
- [x] سایز تصویر 1200x630 است
- [x] contentType درست است (image/png)
- [x] Build موفق بود
- [ ] Deploy در Vercel
- [ ] تست در Facebook Debugger
- [ ] تست در واتساپ

## 🎨 مشخصات تصویر OG:

- **سایز:** 1200x630 پیکسل (استاندارد)
- **فرمت:** PNG
- **رنگ:** Gradient بنفش (#667eea → #764ba2)
- **محتوا:**
  - ✍️ ایموجی امضا
  - عنوان فارسی و آلمانی
  - توضیحات کوتاه
  - دامنه سایت

## 🚀 Deploy:

```bash
# Commit تغییرات
git add app/layout.tsx app/opengraph-image.tsx
git commit -m "Fix: Add metadataBase and update OG image for social media preview"

# Push به repository
git push origin main

# Vercel به صورت خودکار deploy میکنه
```

## 📝 نکات مهم:

1. **Cache واتساپ:** اگر تصویر قدیمی رو میبینی، cache issue هست. صبر کن یا از debugger استفاده کن.

2. **زمان بارگذاری:** اولین باری که لینک رو share میکنی، واتساپ باید تصویر رو fetch کنه. ممکنه 2-3 ثانیه طول بکشه.

3. **HTTPS:** حتماً از HTTPS استفاده کن. واتساپ تصاویر HTTP رو نمایش نمیده.

4. **حجم تصویر:** Next.js به صورت خودکار تصویر رو generate و optimize میکنه. نگران حجم نباش.

## 🎉 نتیجه:

حالا وقتی لینک رو در واتساپ share میکنی، باید یک preview زیبا با:

- ✅ تصویر بنفش با gradient
- ✅ عنوان سایت
- ✅ توضیحات
- ✅ ایموجی و دامنه

نمایش داده بشه! 🚀

---

**سوال داری؟** اگر بعد از deploy هنوز مشکل داری، بهم بگو تا بررسی کنیم.
