# راهنمای امنیتی سایت / Security Guide

## اقدامات امنیتی پیاده‌سازی شده

### 1. هدرهای امنیتی (Security Headers)

✅ Content-Security-Policy (CSP) - جلوگیری از XSS و حملات تزریق کد
✅ Strict-Transport-Security (HSTS) - اجبار استفاده از HTTPS برای 2 سال
✅ X-Frame-Options: DENY - جلوگیری از clickjacking
✅ X-Content-Type-Options: nosniff - جلوگیری از MIME sniffing
✅ X-XSS-Protection - محافظت در برابر XSS
✅ Referrer-Policy: no-referrer - حفظ حریم خصوصی
✅ Permissions-Policy - غیرفعال‌سازی دوربین، میکروفون، موقعیت جغرافیایی

### 2. Rate Limiting

✅ محدودیت 100 درخواست در دقیقه برای صفحات معمولی
✅ محدودیت 10 درخواست در دقیقه برای API routes
✅ بلاک خودکار درخواست‌های مشکوک

### 3. محافظت در برابر حملات رایج

✅ بلاک الگوهای مشکوک (.env, .git, wp-admin, phpMyAdmin، و...)
✅ بررسی User-Agent برای شناسایی bot های مخرب
✅ Validation و Sanitization تمام ورودی‌های کاربر
✅ جلوگیری از XSS، SQL Injection، Path Traversal

### 4. حفاظت از اطلاعات حساس

✅ حذف هدرهای شناسایی سرور (X-Powered-By)
✅ عدم افشای اطلاعات خطا به کاربر
✅ محافظت از API Keys در environment variables
✅ عدم ذخیره‌سازی هیچ داده کاربر
✅ بدون استفاده از کوکی‌ها

### 5. CSRF Protection

✅ Cross-Origin-Embedder-Policy: require-corp
✅ Cross-Origin-Opener-Policy: same-origin
✅ Cross-Origin-Resource-Policy: same-origin

### 6. محافظت API

✅ Validation محتوای JSON
✅ محدودیت حجم درخواست (max 50KB)
✅ Sanitization خروجی
✅ Rate limiting اختصاصی API

## توصیه‌های امنیتی اضافی

### استفاده از HTTPS

🔒 **حتماً** از SSL/TLS استفاده کنید:

```bash
# برای محیط توسعه:
npm install -D mkcert
mkcert -install
mkcert localhost
```

### محیط Production

در محیط تولید حتماً:

1. از CDN با DDoS protection استفاده کنید (Cloudflare)
2. Firewall قوی داشته باشید (WAF)
3. لاگ تمام درخواست‌ها را نگهداری کنید
4. از monitoring برای تشخیص حملات استفاده کنید

### نظارت و مانیتورینگ

- نصب ابزارهای مانیتورینگ برای تشخیص حملات DDoS
- بررسی منظم لاگ‌ها برای فعالیت‌های مشکوک
- تنظیم alerts برای تعداد درخواست‌های غیرمعمول

### پیکربندی‌های توصیه شده

#### Cloudflare (توصیه می‌شود)

1. فعال کردن "Under Attack Mode" در صورت حمله
2. تنظیم Rate Limiting اضافی
3. استفاده از Bot Fight Mode
4. فعال‌سازی Email Address Obfuscation
5. فعال‌سازی Automatic HTTPS Rewrites

#### محیط Server

```bash
# نصب fail2ban برای بلاک IP های مخرب
sudo apt install fail2ban

# تنظیم UFW firewall
sudo ufw enable
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp
```

### Backup و Recovery

- گرفتن backup منظم از کد
- داشتن برنامه disaster recovery
- تست منظم بازیابی

## چک‌لیست امنیتی روزانه

- [ ] بررسی لاگ‌های غیرمعمول
- [ ] بررسی ترافیک برای حملات DDoS
- [ ] بررسی API usage برای فعالیت مشکوک
- [ ] به‌روزرسانی dependencies

## چک‌لیست امنیتی ماهانه

- [ ] تغییر API keys
- [ ] بررسی آسیب‌پذیری‌های جدید
- [ ] به‌روزرسانی تمام پکیج‌ها
- [ ] بررسی logs برای الگوهای حمله
- [ ] تست penetration testing

## دستورات مفید

### بررسی آسیب‌پذیری‌ها

```bash
npm audit
npm audit fix
```

### تست امنیت هدرها

```bash
curl -I https://your-domain.com
```

### مانیتورینگ لاگ‌ها

```bash
# در production با PM2
pm2 logs

# بررسی خطاها
pm2 logs --err
```

## منابع بیشتر

- OWASP Top 10: https://owasp.org/www-project-top-ten/
- Mozilla Observatory: https://observatory.mozilla.org/
- Security Headers: https://securityheaders.com/

## در صورت حمله

1. فعال‌سازی "Under Attack Mode" در Cloudflare
2. بررسی لاگ‌ها برای شناسایی منبع حمله
3. بلاک IP ranges مخرب
4. افزایش rate limiting
5. مشاوره با متخصص امنیتی

---

**نکته مهم:** این تنظیمات پایه امنیتی هستند. برای امنیت کامل، نیاز به لایه‌های امنیتی اضافی در سطح زیرساخت (CDN, WAF, DDoS Protection) دارید.
